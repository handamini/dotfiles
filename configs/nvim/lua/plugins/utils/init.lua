-- Utils
local comment = require("plugins.utils.comment")
local markdown_preview = require("plugins.utils.markdown-preview")
local nvim_autopairs = require("plugins.utils.nvim-autopairs")

return {
    comment,
    markdown_preview,
    nvim_autopairs,
}
